extends layout

block content
  h1= title
  p This website was created using Express, Node.js and MongoDB. 

  h2 Records

  if error
    p Error getting content.
  else
    p The library has the following record counts:

    ul
      li #[strong Books:] !{data.book_count}
      li #[strong Copies:] !{data.copy_count}
      li #[strong Copies available:] !{data.copy_available_count}
      li #[strong Authors:] !{data.author_count}
      li #[strong Categories:] !{data.category_count}